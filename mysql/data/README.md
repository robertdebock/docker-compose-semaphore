The files created here (ending in .sql) will be imported into a newly deployed MySQL instance.

The semaphore.sql file contains the structure for MySQL so semaphore can start up. It's empty by default.
